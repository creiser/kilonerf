render:
    fast_sampling: True # this activates a better implementation of empty space skipping and early ray termination
    max_samples_per_ray: 8
    transmittance_threshold: 0.01

performance_monitoring: False
render_only: True
testskip: 1
